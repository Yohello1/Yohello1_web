#include <stdio.h>
#include <string.h>
int tpoint(void);
int slopepluspoint(void);
int main()
{
    //Commands 
    printf("This is slope calculator v0.2\n");
    printf("2 points to slope = 2point\ntaking 1 point and slope to y-intercept = 1point\n");
    //makes answers and such
    char check[7];
    char ptoslope[7] = "1point";
    char dam[7] = "2point";
    //gets input
    scanf("%s", check);
    //Sends to right function depending on what it wants
    if(strcmp(check,dam) == 0)
    {
    tpoint();
    }
    if(strcmp(check,ptoslope) == 0 )
    {
    slopepluspoint();
    }
}
int slopepluspoint(void)
{
    //makes x and y
    printf("What is the point(x,y) then the slope?\n");
    int x1;
    int y1;
    //Gets them
    scanf("%d",&x1);
    scanf("%d",&y1);
    //prints them
    printf("%d,%d\n",x1,y1);
    printf("Slope?\n");
    //does the math and prints
    float slope;
    scanf("%f", &slope);
    float tbu = x1 * slope;
    float tbu2 = y1 - tbu;
    y1 = tbu2;
    printf("%i\n", y1);
}
int tpoint(void)
{
    //Makes vars
    printf("point 1 (put x then y)\n");
    int y1;
    int y2;
    int x1;
    int x2;
    //Gets and prints vars
    scanf("%d", &x1);
    scanf("%d", &y1);
    printf("point 2\n");
    scanf("%d", &x2);
    scanf("%d", &y2);
    printf("(%d,%d) (%d,%d) these are the points\n", x1, y1, x2, y2);
    float bottom = x1 - x2;
    float top = y1 - y2;
    float answer = top / bottom;
    float tbu = x1 * answer;
    float tbu2 = y1 - tbu;
    y1 = tbu2;
    printf("slope = %.2f\n \n %.2f\n -------  X  + %i\n %.2f\n \n", answer, top, y1, bottom);
    printf("\n \n y = %.2f x + %d\n", answer, y1);
    return 0;
}